export interface IPost {
  _id: string;
  title: string;
  description: string;
  visible: boolean;
}
export interface INewPost {
  title: string;
  description: string;
  visible: boolean;
}

export interface IOnePost {
  _id: string;
  title: string;
  description: string;
  visible: boolean;
}
